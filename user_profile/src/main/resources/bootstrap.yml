spring:
  application:
    name: user_profile
  cloud:
    config:
      uri: ${SPRING_CLOUD_CONFIG_URI:http://localhost:8071}
      fail-fast: false
      retry:
        max-attempts: 6
        initial-interval: 2000
        max-interval: 3000
        multiplier: 1.1
